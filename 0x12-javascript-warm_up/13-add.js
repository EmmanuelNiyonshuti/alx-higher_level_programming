#!/usr/bin/node
/*
  Define and export a function named 'add' that takes two parameters 'a' and 'b'.
  This function returns the sum of the two parameters.
*/
exports.add = function (a, b) {
  return (a + b);
};
